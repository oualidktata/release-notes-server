const {getLinks} =require("../dataService");
const { assert } = require("chai");
describe('should return links', () => {
    
    it("empty array",()=>{
    
        let res=getLinks("v1-0001");


      //  assert(res).toBe([])

    })
    

});